Steps:

If you don't see a node_packager folder, run a command prompt to current directory
and type: npm install

That will install all the packages needed for the current set-up in node.js

To run:

In command prompt on this directory, type: node main.js